# Holistic Discovery — Delta (2026-05-06)

Compared against the previous snapshot of `graph.json`.

## New nodes (2)

- `Concept` **Interconnectedness**
- `Concept` **Repositories**

## New edges (4)

- `MENTIONS` repo:McFuzzySquirrel/ai-etcetera → concept:interconnectedness (confidence 0.95, via `concept_extractor`)
- `MENTIONS` repo:McFuzzySquirrel/ai-etcetera → concept:phase (confidence 0.95, via `concept_extractor`)
- `MENTIONS` repo:McFuzzySquirrel/ai-etcetera → concept:repositories (confidence 0.80, via `concept_extractor`)
- `MENTIONS` repo:McFuzzySquirrel/ai-etcetera → concept:what (confidence 0.95, via `concept_extractor`)

